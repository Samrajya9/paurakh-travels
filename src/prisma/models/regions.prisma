model Region {
    id   String @id @default(cuid())
    name String @unique // ← "Everest Region" twice is a data bug

    destinations Destination[]

    createdAt DateTime @default(now()) @map("created_at")
    updatedAt DateTime @updatedAt @map("updated_at")

    @@map("regions")
}
